@model TCSH.Models.AgeType

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>AgeType</h4>
<hr />
@{
    if (ViewBag.failed != null)
    {
        <div class="alert alert-danger" role="alert">
            <i class="bi bi-exclamation-triangle-fill"></i>
  please reach all requirments!
        </div>
    }
    if (ViewBag.failedWithTitle != null)
    {
        <div class="alert alert-danger" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i>
          There is Same Name!!
        </div>
    }
}
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
           @* <div class="form-group">
                <label asp-for="AgeTypeId" class="control-label"></label>
                <input asp-for="AgeTypeId" class="form-control" />
                <span asp-validation-for="AgeTypeId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="Type_Title" class="control-label"></label>
                <input asp-for="Type_Title" class="form-control" />
                <span asp-validation-for="Type_Title" class="text-danger"></span>
            </div>
         
            <br/><div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
